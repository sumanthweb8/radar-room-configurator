# Core deps for the FastAPI server + the three importers.
# Pins are loose ranges so this file co-exists with other Python projects
# in the same env (e.g. feast) — pin tighter only if you hit a regression.
anthropic>=0.94
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.7
pillow-heif>=0.16
Pillow>=10.3
python-dotenv>=1.0

# Metaroom PDF importer
pypdf>=4

# DXF importer (also brings numpy as a transitive dep)
ezdxf>=1.3
