# Core deps for the FastAPI server + the two offline importers.
# Pins are loose ranges so this file co-exists with other Python projects
# in the same env — pin tighter only if you hit a regression.
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Metaroom PDF importer
pypdf>=4

# DXF importer (also brings numpy as a transitive dep)
ezdxf>=1.3
